# tests/e2e

Playwright end-to-end user journeys (registration, budgeting, debt planning,
report export). Introduced once `apps/web` has enough interactive surface to
justify browser-driven tests, starting in **Version 0.2**.
