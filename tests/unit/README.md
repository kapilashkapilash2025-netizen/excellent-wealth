# tests/unit

Unit tests for financial formulas, validation schemas, and UI components live
next to their source files inside each package (e.g. `packages/financial-engine/src/*.test.ts`),
which keeps tests close to the code they cover.

This directory is reserved for **cross-package unit tests** that don't belong
to a single workspace package — currently empty.
