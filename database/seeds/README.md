# database/seeds

Seed scripts for local development and the privacy-first local demo mode
(sample accounts, transactions, budgets, and goals — no real user data).
Not implemented yet — `apps/api` (Version 0.2) only has the `User`,
`Session`, and `AuditEvent` tables so far; seed data becomes useful once
Version 0.3+ adds financial-data tables worth demoing.
